name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ghcr.io/asseumrabahpro/atlas-des-tensions

jobs:
  build-and-push:
    name: Build & push Docker images
    runs-on: ubuntu-latest
    # Only run if CI passed
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push API image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ env.IMAGE_NAMESPACE }}/api:latest
            ${{ env.IMAGE_NAMESPACE }}/api:${{ github.event.workflow_run.head_sha }}

      - name: Build & push Web image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ env.IMAGE_NAMESPACE }}/web:latest
            ${{ env.IMAGE_NAMESPACE }}/web:${{ github.event.workflow_run.head_sha }}

  # ── Optional: deploy to a VPS via SSH ──────────────────────────────────────
  # Uncomment and configure once you have a server.
  #
  # deploy-vps:
  #   name: Deploy to VPS
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: SSH & pull latest images
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         script: |
  #           echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
  #           docker compose -f /opt/world-news/docker-compose.prod.yml pull
  #           docker compose -f /opt/world-news/docker-compose.prod.yml up -d
